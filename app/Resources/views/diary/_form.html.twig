{{ form_start(diaryForm) }}
                    {{ form_row(diaryForm.title) }}
                    {{ form_row(diaryForm.note) }}
                    {{ form_row(diaryForm.attachment) }}
<button type="submit" class="btn btn-primary" formnovalidate>{{ 'save'|trans }}</button>
{{ form_end(diaryForm) }}